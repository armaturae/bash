####
# Monitors
# Colon delimited, should be arranged in order from left to right
####

export MONITORS=DP-2:HDMI-1:DP-1
export MONITOR_LEFT=$(echo $MONITORS | cut -d ':' -f 1)
export MONITOR_MAIN=$(echo $MONITORS | cut -d ':' -f 2)
export MONITOR_RIGHT=$(echo $MONITORS | cut -d ':' -f 3)
